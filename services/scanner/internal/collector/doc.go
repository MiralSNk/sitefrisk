// Package collector реализует конкурентный сбор фактов о целевом URL
// через набор независимых источников (Collector), таких как заголовки
// ответа, TLS-конфигурация или чувствительные пути.
//
// RunAll запускает все переданные коллекторы параллельно, ограничивая
// общее время работы через context.WithTimeout, и стримит промежуточные
// и финальный результат через канал Event — это позволяет вызывающему
// коду видеть прогресс сканирования по мере готовности отдельных
// коллекторов, а не только итоговый результат.
//
// TODO(redis/nats): публиковать события EventCollectorDone в очередь
// сообщений, когда в проект будет добавлен брокер.
package collector
